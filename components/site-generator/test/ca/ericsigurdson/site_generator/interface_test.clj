(ns ca.ericsigurdson.site-generator.interface-test
  (:require [clojure.test :as test :refer :all]
            [ca.ericsigurdson.site-generator.interface :as site-generator]))

(deftest dummy-test
  (is (= 1 1)))
