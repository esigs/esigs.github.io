(ns ca.ericsigurdson.file-utils.interface-test
  (:require [clojure.test :as test :refer :all]
            [ca.ericsigurdson.file-utils.interface :as file-utils]))

(deftest dummy-test
  (is (= 1 1)))
