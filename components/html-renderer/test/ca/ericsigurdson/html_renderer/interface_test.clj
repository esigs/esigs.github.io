(ns ca.ericsigurdson.html-renderer.interface-test
  (:require [clojure.test :as test :refer :all]
            [ca.ericsigurdson.html-renderer.interface :as html-renderer]))

(deftest dummy-test
  (is (= 1 1)))
