(ns ca.ericsigurdson.markdown-parser.interface-test
  (:require [clojure.test :as test :refer :all]
            [ca.ericsigurdson.markdown-parser.interface :as markdown-parser]))

(deftest dummy-test
  (is (= 1 1)))
